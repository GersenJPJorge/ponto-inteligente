web: java -Dserver.port=$PORT -Dspring.profiles.active=prod -jar target/ponto-inteligente-0.0.1-SNAPSHOT.jar
// a porta do heroku é dinamica, daí o $PORT
// o .active=prod está justamente dizendo para se baser no application-prod.properties
// o ponto-inteligente-0.0.1-SNAPSHOT é a concatenação do pom.xml - para ter certeza do nome fazemos o seguinte:
// na raiz do projeto execute run as maven install - isso fará o processo de compilação e geração do build.
// ao término ele gera o jar com o nome do artifact
// e ele aparecerá no pasta target do seu projeto
//
